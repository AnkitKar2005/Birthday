# Ambient Music

Place your ambient music file here as `ambient.mp3`.

The site works perfectly without music — it's optional.
If you don't want music, set `musicPath: null` in `src/data/answeha.ts`.
